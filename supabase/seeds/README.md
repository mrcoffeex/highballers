# Seeds

Optional dev/staging data. Same naming as migrations:

**`YYYYMMDDHHMMSS_description.sql`**

Run manually in Supabase SQL Editor **after** schema + migrations, in filename order.

## New seed

```bash
./scripts/db-new-seed.sh demo-users
```
